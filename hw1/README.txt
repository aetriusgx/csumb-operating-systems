
To make sure your .txt file for homework 1 is formatted
correctly, you can run the awk file preproc.awk at the command line
as follows:

$ awk -f preproc.awk hw1.txt

The output will show your solution for each problem.
